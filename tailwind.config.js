/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./public/**/*.html", // Include all HTML files in the `public` folder
    "./src/**/*.{html,js}" // Include any files in the `src` folder (if applicable)
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
